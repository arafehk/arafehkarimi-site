<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {% set defaultTitle = "Arafeh Karimi | Cernavia Studio — Independent Judgement & Sensemaking" %}
    {% set defaultDescription = "Independent judgement, research, and advisory work across AI, education, and public-interest systems." %}
    {% set pageTitle = seo_title or (title ~ " | " ~ site.title) or defaultTitle %}
    {% set pageDescription = seo_description or description or defaultDescription %}
    {% set og_image = og_image or "/assets/img/og-image.jpg" %}
    {% set pageUrl = site.url ~ page.url if site.url and page.url else "" %}
    {% set isArticle = page.url and page.url != "/articles/" and page.url.indexOf("/articles/") == 0 %}

    <title>{{ pageTitle }}</title>
    <meta name="description" content="{{ pageDescription }}">

    {% if pageUrl %}
      <link rel="canonical" href="{{ pageUrl }}">
    {% endif %}

    <meta name="robots" content="index,follow">

    <meta property="og:type" content="{% if isArticle %}article{% else %}website{% endif %}">
    <meta property="og:title" content="{{ pageTitle }}">
    <meta property="og:description" content="{{ pageDescription }}">
    {% if pageUrl %}
      <meta property="og:url" content="{{ pageUrl }}">
    {% endif %}
    <meta property="og:site_name" content="{{ site.title }}">
    <meta property="og:image" content="{{ site.url }}{{ og_image }}">

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ pageTitle }}">
    <meta name="twitter:description" content="{{ pageDescription }}">
    <meta name="twitter:image" content="{{ site.url }}{{ og_image }}">

    <meta name="theme-color" content="#111111">

    <link rel="icon" type="image/png" sizes="32x32" href="/assets/img/favicon-32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/assets/img/favicon-16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/img/apple-touch-icon.png">

    <link rel="stylesheet" href="/assets/css/site.css">

    {% if isArticle and title %}
      <script type="application/ld+json">
        {
          "@context": "https://schema.org",
          "@type": "Article",
          "headline": {{ title | dump | safe }},
          "description": {{ pageDescription | dump | safe }},
          "author": {
            "@type": "Person",
            "name": "Arafeh Karimi"
          },
          "publisher": {
            "@type": "Organization",
            "name": "Cernavia Studio"
          }{% if date %},
          "datePublished": "{{ date | date('yyyy-MM-dd') }}",
          "dateModified": "{{ date | date('yyyy-MM-dd') }}"{% endif %}{% if pageUrl %},
          "mainEntityOfPage": {
            "@type": "WebPage",
            "@id": "{{ pageUrl }}"
          }{% endif %}
        }
      </script>
    {% endif %}
  </head>
  <body>
    {% include "partials/header.njk" %}
    <main>
      {{ content | safe }}
    </main>
    {% include "partials/footer.njk" %}
    <script src="/assets/js/site.js" defer></script>
  </body>
</html>